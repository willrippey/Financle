## Packages
framer-motion | Complex animations for game reveals and page transitions
cmdk | Accessible command menu for the company search autocomplete
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
Authentication uses Replit Auth (provided via use-auth hook)
Game logic relies on `api.games.*` endpoints
Images for companies (logos) might need a fallback placeholder if not available
